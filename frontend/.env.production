VITE_API_URL=https://ton-projet.up.railway.app/api
```

> ⚠️ Ne commit pas ce fichier si tu as des secrets dedans — mais `VITE_API_URL` est public donc pas de souci.

---

## 3. Variable d'environnement sur Vercel

Dans **Vercel → ton projet → Settings → Environment Variables** :
```
Name   : VITE_API_URL
Value  : https://ton-projet.up.railway.app/api